global:
  resolve_timeout: 5m

route:
  group_by:
    - alertname
  group_wait: 10s
  group_interval: 10s
  repeat_interval: 1h
  receiver: default

receivers:
  - name: default

    email_configs:
    - to: "petartornjanski@gmail.com"
      from: "petartornjanski@gmail.com"
      smarthost: "smtp.google.com:587"
      auth_username: "petartornjanski@gmail.com"
      auth_identity: "petartornjanski@gmail.com"
      auth_password: "{{ gmail_password }}"
